subprojects {
    apply plugin: 'java'

    repositories {
        mavenCentral()
    }

    test {
        beforeTest {
            descriptor -> logger.lifecycle("Running test: ${descriptor}")
        }
        
        testLogging {
            showStandardStreams = true
        }
    }
}
