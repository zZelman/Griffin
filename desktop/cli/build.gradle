apply plugin: 'java'
apply plugin: 'application'

mainClassName = "com.griffin.desktop.cli.Cli"

repositories {
    mavenCentral()
}

dependencies {
    compile project(':core')
}

run {
    standardInput = System.in
    args 'server_list.ini desktop chain'.split(' ')
}
