apply plugin: 'java'
apply plugin: 'application'

mainClassName = "com.griffin.desktop.cli.Main"

repositories {
    mavenCentral()
}

dependencies {
    compile project(':core')
}

run {
    standardInput = System.in
    args 'server_list.ini desktop prefix prefix prefix hello world help'.split(' ')
}
