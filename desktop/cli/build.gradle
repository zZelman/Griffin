apply plugin: 'application'

mainClassName = "com.griffin.desktop.cli.Main"

dependencies {
    compile project(':core')
}

run {
    standardInput = System.in
    args 'server_list.ini desktop help help'.split(' ')
}
