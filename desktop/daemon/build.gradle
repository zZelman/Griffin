apply plugin: 'java'
apply plugin: 'application'

mainClassName = "com.griffin.desktop.daemon.Daemon"

repositories {
    mavenCentral()
}

dependencies {
    compile project(':core')
}

run {
    standardInput = System.in
    args 'server_list.ini desktop'.split(' ')
}
