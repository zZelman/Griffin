apply plugin: 'java'
apply plugin: 'application'

mainClassName = "com.griffin.nameserver.Nameserver"

repositories {
    mavenCentral()
}

dependencies {
    compile project(':core')
}

run {
    standardInput = System.in
    args 'server_list.ini'.split(' ')
}